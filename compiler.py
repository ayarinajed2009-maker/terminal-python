#compiler v0.1 beta
#You can find Help page to know how to use the terminal
#run with run.bat
from functions.system import *
from functions.video import *
for i in range(10):
    notify(str(generate_chaine(10)), str(generate_code(10)))